Repair the type errors in this package while preserving its specified runtime behavior. Change the source as needed.

Do not weaken type checking, suppress diagnostics, introduce unrestricted Any, remove required functionality, or alter tests and checker configuration.

Use patch_and_check to apply unified diffs and validate changes. You may edit several allowed source files in one call. Paths are relative to the task root. Report the final state accurately. If work remains incomplete, say what remains.

INITIAL TYPE-CHECKER REPORT
=== TYPE-CHECKER REPORT (EXPANDED) ===
---
occurrence_index: 1
severity: error
file: pkg/handler_a.py
line: 9
col: 18
code: arg-type
message: Argument 1 to "dispatch" has incompatible type "AlphaHandler"; expected "Handler"
extras: {"hint": null}
---
occurrence_index: 2
severity: error
file: pkg/handler_a.py
line: 10
col: 18
code: arg-type
message: Argument 1 to "dispatch" has incompatible type "AlphaHandler"; expected "Handler"
extras: {"hint": null}
---
occurrence_index: 0
severity: error
file: pkg/handler_b.py
line: 9
col: 20
code: arg-type
message: Argument 1 to "dispatch" has incompatible type "BetaHandler"; expected "Handler"
extras: {"hint": null}
---
--- 3 error(s), 3 record(s) total ---

SOURCE, PUBLIC TESTS, AND CONFIGURATION
# FILE: pkg/__init__.py
```

```

# FILE: pkg/handler_a.py
```
from pkg.proto import dispatch

class AlphaHandler:
    def process(self, msg: str) -> str:
        return f"A:{msg}"

def run_a() -> str:
    h = AlphaHandler()
    d1 = dispatch(h, "1")
    d2 = dispatch(h, "2")
    return d1 + d2

```

# FILE: pkg/handler_b.py
```
from pkg.proto import dispatch

class BetaHandler:
    def process(self, msg: str) -> str:
        return f"B:{msg}"

def run_b() -> str:
    h = BetaHandler()
    return dispatch(h, "3")

```

# FILE: pkg/proto.py
```
from typing import Protocol

class Handler(Protocol):
    def handle(self, msg: str) -> str: ...

def dispatch(h: Handler, msg: str) -> str:
    return h.handle(msg)

```

# FILE: tests/test_handlers.py
```
from pkg.handler_a import AlphaHandler
from pkg.handler_b import BetaHandler

def test_a():
    assert AlphaHandler().process("1") == "A:1"

def test_b():
    assert BetaHandler().process("3") == "B:3"

```

