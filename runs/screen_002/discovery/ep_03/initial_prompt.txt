Repair the type errors in this package while preserving its specified runtime behavior. Change the source as needed.

Do not weaken type checking, suppress diagnostics, introduce unrestricted Any, remove required functionality, or alter tests and checker configuration.

Use patch_and_check to apply source patches and validate changes. You may edit several allowed source files in one call. Paths are relative to the task root. Report the final state accurately. If work remains incomplete, say what remains.

INITIAL TYPE-CHECKER REPORT
=== TYPE-CHECKER REPORT ===
===
severity: error
code: return-value
message: Incompatible return value type (got "str", expected "int")
extras: {"hint": null}
multiplicity: 2
  LOC|{"col": 11, "file": "pkg/db_config.py", "line": 7, "occurrence_index": 2}
  LOC|{"col": 11, "file": "pkg/web_config.py", "line": 7, "occurrence_index": 0}
===
severity: error
code: typeddict-item
message: Incompatible types (expression has type "int", TypedDict item "port" has type "str")
extras: {"hint": null}
multiplicity: 1
  LOC|{"col": 29, "file": "pkg/db_config.py", "line": 10, "occurrence_index": 3}
===
severity: error
code: typeddict-item
message: Incompatible types (expression has type "int", TypedDict item "workers" has type "str")
extras: {"hint": null}
multiplicity: 1
  LOC|{"col": 33, "file": "pkg/web_config.py", "line": 10, "occurrence_index": 1}
===
--- 4 error(s), 4 record(s) total ---

SOURCE, PUBLIC TESTS, AND CONFIGURATION
# FILE: pkg/__init__.py
```

```

# FILE: pkg/db_config.py
```
from typing import TypedDict

class DBConfig(TypedDict):
    port: str

def get_db_port(cfg: DBConfig) -> int:
    return cfg["port"]

def run_db() -> int:
    cfg: DBConfig = {"port": 5432}
    p1 = get_db_port(cfg)
    p2 = get_db_port(cfg)
    return p1 + p2

```

# FILE: pkg/web_config.py
```
from typing import TypedDict

class WebConfig(TypedDict):
    workers: str

def get_workers(cfg: WebConfig) -> int:
    return cfg["workers"]

def run_web() -> int:
    cfg: WebConfig = {"workers": 4}
    return get_workers(cfg)

```

# FILE: tests/test_settings.py
```
from pkg.db_config import run_db
from pkg.web_config import run_web

def test_settings():
    assert run_db() == 10864
    assert run_web() == 4

```

